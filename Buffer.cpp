//
// Created by eleno on 03.05.2021.
//

#include "Buffer.h"
/*
template<class T>
int Buffer<T>::Add(T *obj) {
  if (free_id_.empty()) {
    buf_.push_back(obj);
    return buf_.size() - 1;
  } else {
    int id = free_id_[free_id_.size() - 1];
    buf_[id] = obj;
    free_id_.pop_back();
    return id;
  }
}

template<class T>
void Buffer<T>::Erase(int id) {
  buf_[id] = nullptr;
  free_id_.push_back(id);
}

template<class T>
Buffer<T>::~Buffer<T>() {

}*/